#include "Cmm.h"

Ghclib_aToWordzh (P_ clos)
{
    return (clos);
}

Ghclib_reallyUnsafePtrEqualityUpToTag (W_ clos1, W_  clos2)
{
    clos1 = UNTAG(clos1);
    clos2 = UNTAG(clos2);
    return (clos1 == clos2);
}
